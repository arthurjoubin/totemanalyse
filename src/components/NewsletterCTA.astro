---
interface Props {
  variant?: 'inline' | 'box';
}

const { variant = 'inline' } = Astro.props;
---

{variant === 'box' ? (
  <div class="my-8 p-6 bg-gray-50 border border-gray-200 rounded text-center">
    <p class="text-sm text-gray-600 mb-3">Vous aimez ce contenu ?</p>
    <p class="font-semibold text-gray-900 mb-4">Recevez nos analyses directement dans votre boîte mail</p>
    <a href="https://totemanalyse.substack.com/subscribe"
       target="_blank"
       rel="noopener noreferrer"
       class="inline-block py-2 px-6 bg-totem-green text-white text-sm font-medium hover:bg-totem-light transition-colors">
      S'abonner gratuitement →
    </a>
  </div>
) : (
  <div class="my-6 py-4 px-5 border-l-4 border-totem-green bg-gray-50">
    <p class="text-sm text-gray-700">
      <span class="font-semibold">📬 Newsletter Totem Analyse</span> — Recevez nos analyses approfondies chaque mois.
      <a href="https://totemanalyse.substack.com/subscribe"
         target="_blank"
         rel="noopener noreferrer"
         class="text-totem-green font-medium hover:underline ml-1">
        S'abonner →
      </a>
    </p>
  </div>
)}
